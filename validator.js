// Generated by CoffeeScript 1.9.3
(function() {
  var check_email, check_is_capital, check_is_number, check_long_enough;

  check_email = function(email) {
    return /[a-zA-Z0-9-._]+@[a-zA-Z0-9-._]+\.[a-zA-Z0-9-._]/.test(email);
  };

  check_is_number = function(password) {
    return /\d/.test(password);
  };

  check_long_enough = function(password) {
    return password.length >= 8;
  };

  check_is_capital = function(password) {
    return /[A-Z]/.test(password);
  };

  $(document).on('ready page:load', function() {
    $('.errors li').hide();
  });

  $(function() {
    $("form").submit = function(event) {
      var email, is_valid, password;
      is_valid = false;
      $(".errors li").hide;
      email = $("input[placeholder='Email']").val;
      password = $("input[placeholder='Password']").val;
      if (!check_email(email)) {
        is_valid = false;
        $(".errors li:nth-child(1)").show();
      }
      if (!check_long_enough(password)) {
        is_valid = false;
        $(".errors li:nth-child(2)").show();
      }
      if (!check_is_capital(password)) {
        is_valid = false;
        $(".errors li:nth-child(3)").show();
      }
      if (!check_is_number(password)) {
        is_valid = false;
        $(".errors li:nth-child(4)").show();
      }
      if (!is_valid) {
        event.stopPropagation;
        event.preventDefault;
      }
    };
  });

}).call(this);
